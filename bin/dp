#!/usr/bin/env ruby
# frozen_string_literal: true

require 'dynprog'

display = DP::Display.new(20, 40)
display.reset_position

display.append(DP::VERSION)
sleep(2)

display.position(4, 2)
display.append("Dynamic Programming")
sleep(2)

display.close
